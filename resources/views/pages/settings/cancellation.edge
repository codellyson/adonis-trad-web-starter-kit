@layout('layouts/app')

@set('title', 'Cancellation Policy - BookMe')

@section('content')
  <div class="max-w-2xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
    <div class="mb-8">
      <a href="{{ route('settings.index') }}" class="inline-flex items-center text-sm text-sand-11 hover:text-sand-12 mb-4">
        <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"/>
        </svg>
        Back to Settings
      </a>
      <h1 class="text-2xl font-bold text-sand-12">Cancellation Policy</h1>
      <p class="text-sand-11 mt-1">Set rules for cancellations and rescheduling</p>
    </div>

    <form method="POST" action="{{ route('settings.cancellation.update') }}" class="bg-white border border-sand-7 rounded-xl p-6 space-y-6">
      {{ csrfField() }}

      <div>
        <label for="cancellationHours" class="block text-sm font-medium text-sand-12 mb-1">Cancellation Notice Period *</label>
        <div class="flex items-center gap-3">
          <input
            type="number"
            id="cancellationHours"
            name="cancellationHours"
            value="{{ business.cancellationHours }}"
            min="0"
            max="168"
            required
            class="w-24 px-4 py-2.5 border border-sand-7 rounded-lg focus:outline-none focus:ring-2 focus:ring-primary/20 focus:border-primary"
          />
          <span class="text-sand-11">hours before appointment</span>
        </div>
        <p class="text-xs text-sand-11 mt-1">Customers must cancel at least this many hours in advance. Set to 0 for no restriction.</p>
      </div>

      <div>
        <label for="cancellationPolicy" class="block text-sm font-medium text-sand-12 mb-1">Policy Description</label>
        <textarea
          id="cancellationPolicy"
          name="cancellationPolicy"
          rows="4"
          class="w-full px-4 py-2.5 border border-sand-7 rounded-lg focus:outline-none focus:ring-2 focus:ring-primary/20 focus:border-primary"
          placeholder="Describe your cancellation and refund policy..."
        >{{ business.cancellationPolicy || '' }}</textarea>
        <p class="text-xs text-sand-11 mt-1">This will be shown to customers when they book</p>
      </div>

      <div class="bg-sand-2 rounded-lg p-4">
        <h4 class="font-medium text-sand-12 mb-2">Policy Preview</h4>
        <div class="text-sm text-sand-11">
          @if(business.cancellationHours > 0)
            <p>• Cancellations must be made at least <strong>{{ business.cancellationHours }} hours</strong> before the appointment</p>
          @else
            <p>• Cancellations can be made at any time before the appointment</p>
          @endif
          @if(business.cancellationPolicy)
            <p class="mt-2">{{ business.cancellationPolicy }}</p>
          @endif
        </div>
      </div>

      <div class="flex items-center gap-3 pt-4">
        <button type="submit" class="px-6 py-2.5 bg-primary text-white font-medium rounded-lg hover:bg-primary/90">
          Save Changes
        </button>
        <a href="{{ route('settings.index') }}" class="px-6 py-2.5 border border-sand-7 text-sand-12 font-medium rounded-lg hover:bg-sand-2">
          Cancel
        </a>
      </div>
    </form>
  </div>
@endsection

